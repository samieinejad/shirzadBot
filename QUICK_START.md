# 🚀 راهنمای سریع نصب و راه‌اندازی

## مشکل: Python نصب نشده است

Python روی کامپیوتر شما نصب نیست. این راهنما به شما کمک می‌کند آن را نصب کنید.

---

## ✅ روش ۱: نصب Python از مایکروسافت استور (توصیه می‌شود)

### مراحل:
1. **باز کردن استور:**
   - دکمه `Windows` را فشار دهید
   - "Microsoft Store" را جستجو و باز کنید

2. **جستجوی Python:**
   - در استور، "Python 3.11" یا "Python 3.12" را جستجو کنید
   - نسخه رسمی Python را نصب کنید

3. **تأیید نصب:**
   - PowerShell یا Command Prompt را باز کنید
   - دستور زیر را اجرا کنید:
     ```
     python --version
     ```
   - باید نسخه Python نمایش داده شود

---

## ✅ روش ۲: نصب Python از وب‌سایت رسمی

### مراحل:
1. **دانلود Python:**
   - به آدرس زیر بروید:
     ```
     https://www.python.org/downloads/
     ```
   - روی دکمه "Download Python 3.x.x" کلیک کنید

2. **نصب Python:**
   - فایل دانلود شده را اجرا کنید
   - ⚠️ **حتما** تیک "Add Python to PATH" را فعال کنید
   - روی "Install Now" کلیک کنید

3. **تأیید نصب:**
   - PowerShell جدید باز کنید
   - دستورات زیر را اجرا کنید:
     ```
     python --version
     pip --version
     ```

---

## ✅ روش ۳: استفاده از Anaconda (برای کاربران پیشرفته)

اگر از Anaconda استفاده می‌کنید:

```bash
# نصب با conda
conda create -n shirzadBot python=3.11
conda activate shirzadBot
pip install -r requirements.txt
python app.py
```

---

## 🔧 پس از نصب Python

### مرحله ۱: بررسی نصب
```bash
# PowerShell را باز کنید و این دستورات را اجرا کنید:
python --version
pip --version
```

### مرحله ۲: نصب وابستگی‌های برنامه

#### گزینه الف: استفاده از فایل Batch (پیشنهادی)
```batch
install_simple.bat
```

#### گزینه ب: نصب دستی
```bash
# در پوشه پروژه:
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

### مرحله ۳: تنظیم توکن‌ها
فایل `app.py` را باز کنید و خطوط 135-141 را ویرایش کنید:

```python
TELEGRAM_BOT_TOKEN = "YOUR_TOKEN_HERE"
BALE_BOT_TOKEN = "YOUR_TOKEN_HERE"
ITA_BOT_TOKEN = "YOUR_TOKEN_HERE"

OWNER_ID = YOUR_TELEGRAM_ID
BALE_OWNER_ID = YOUR_BALE_ID
ITA_OWNER_ID = "YOUR_ITA_ID"
```

### مرحله ۴: اجرای برنامه

#### گزینه الف: استفاده از Batch
```batch
run_simple.bat
```

#### گزینه ب: اجرای دستی
```bash
python app.py
```

### مرحله ۵: باز کردن در مرورگر
```
http://localhost:5000
```

---

## ❓ مشکلات رایج

### مشکل: "python is not recognized"
**راه حل:** 
1. PATH را بررسی کنید
2. PowerShell را ببندید و دوباره باز کنید
3. اگر باز هم کار نکرد، Python را دوباره نصب کنید و حتماً "Add to PATH" را فعال کنید

### مشکل: "pip is not recognized"
**راه حل:**
```bash
python -m pip --version
```
اگر کار کرد، از `python -m pip` به جای `pip` استفاده کنید

### مشکل: "permission denied" در نصب پکیج
**راه حل:**
```bash
# اجرای PowerShell به عنوان Administrator:
# کلیک راست روی PowerShell > Run as Administrator
```

### مشکل: خطای "module not found"
**راه حل:**
```bash
# حذف و نصب مجدد محیط مجازی:
rmdir /s venv
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

---

## 📞 نیاز به کمک؟

اگر مشکلاتی دارید:
1. فایل `app.log` را بررسی کنید
2. خطاها را با ما به اشتراک بگذارید
3. مطمئن شوید Python نسخه 3.8 یا بالاتر است

---

## ✅ لیست وابستگی‌ها

این پکیج‌ها به صورت خودکار نصب می‌شوند:

- Flask (وب‌سرور)
- python-telegram-bot (ربات تلگرام)
- pandas (تحلیل داده)
- openpyxl (فایل‌های Excel)
- requests (درخواست‌های HTTP)
- APScheduler (برنامه‌ریزی)
- Pillow (پردازش تصویر)
- jdatetime (تاریخ شمسی)
- pytz (منطقه زمانی)

---

**موفق باشید! 🎉**

