Run these commands:

cd /var/www/shirzadBot

# Stop bot
sudo systemctl stop shirzadbot

# Discard local RUN_ME_FIRST.sh changes and pull
git checkout -- RUN_ME_FIRST.sh
git pull

# Verify port is 5010 in app.py
grep "port=" app.py

# If still 5000, fix it
sed -i 's/port=5000/port=5010/g' app.py

# Check error logs
tail -50 logs/error.log

# Restart
sudo systemctl start shirzadbot

# Test
sleep 5
curl http://localhost:5010

