# 📥 راهنمای نصب کامل Multi-Bot Platform

## ❗ مشکل: Python نصب نیست

اگر پیغام `pip is not recognized` دریافت کردید، باید ابتدا Python را نصب کنید.

---

## 🎯 روش ۱: نصب از Microsoft Store (پیشنهادی برای ویندوز)

### مراحل:

1. **باز کردن Microsoft Store:**
   - دکمه Windows را فشار دهید
   - `Microsoft Store` را جستجو کنید
   - برنامه را باز کنید

2. **جستجوی Python:**
   - در نوار جستجو: `Python 3.12` یا `Python 3.11` را جستجو کنید
   - یا به این لینک بروید: https://www.microsoft.com/store/productId/9NRWMJP3717K

3. **نصب Python:**
   - روی دکمه **"Get"** یا **"Install"** کلیک کنید
   - منتظر تکمیل نصب بمانید

4. **تست نصب:**
   - PowerShell جدید باز کنید
   - دستور زیر را اجرا کنید:
   ```powershell
   python --version
   ```
   - باید چیزی مثل `Python 3.12.x` نمایش دهد

5. **اجرای نصب بات:**
   ```powershell
   cd D:\dev\workspace\shirzadBot
   install_simple.bat
   ```

---

## 🎯 روش ۲: نصب از وب سایت رسمی Python

### مراحل:

1. **دانلود Python:**
   - به سایت رسمی بروید: https://www.python.org/downloads/
   - روی دکمه **"Download Python 3.12.x"** کلیک کنید

2. **اجرای نصب‌گر:**
   - فایل دانلود شده را باز کنید
   - ⚠️ **مهم**: تیک **"Add Python to PATH"** را بزنید
   - روی **"Install Now"** کلیک کنید

3. **تکمیل نصب:**
   - منتظر تکمیل نصب بمانید
   - پس از اتمام، روی **"Close"** کلیک کنید

4. **PowerShell جدید باز کنید:**
   - تمام پنجره‌های PowerShell قدیمی را ببندید
   - PowerShell جدید باز کنید

5. **تست نصب:**
   ```powershell
   python --version
   ```

6. **اجرای نصب بات:**
   ```powershell
   cd D:\dev\workspace\shirzadBot
   install_simple.bat
   ```

---

## ✅ پس از نصب Python

### ۱. نصب وابستگی‌ها:

```powershell
cd D:\dev\workspace\shirzadBot
install_simple.bat
```

این اسکریپت خودکار:
- ✅ محیط مجازی ایجاد می‌کند
- ✅ Flask نصب می‌کند
- ✅ python-telegram-bot نصب می‌کند
- ✅ Pandas نصب می‌کند
- ✅ OpenPyXL نصب می‌کند
- ✅ و سایر وابستگی‌ها...

### ۲. اجرای بات:

```powershell
run_simple.bat
```

یا:

```powershell
venv\Scripts\python.exe app.py
```

### ۳. باز کردن رابط کاربری:

مرورگر خود را باز کنید و به این آدرس بروید:

```
http://localhost:5000
```

---

## 🔧 عیب‌یابی

### مشکل: Python نصب است اما `pip` کار نمی‌کند

**راه حل:**

```powershell
python -m ensurepip --upgrade
```

سپس:

```powershell
python -m pip install -r requirements.txt
```

---

### مشکل: بعد از نصب Python باز هم `python` پیدا نمی‌شود

**راه حل:**

1. PowerShell را **کاملاً ببندید**
2. PowerShell **جدید** باز کنید
3. دستور `python --version` را دوباره امتحان کنید

اگر باز هم کار نکرد:

```powershell
refreshenv
```

---

### مشکل: ارور "Permission denied"

**راه حل:**

PowerShell را به عنوان Administrator اجرا کنید:

1. دکمه Windows + X
2. "Windows PowerShell (Admin)" را انتخاب کنید
3. دوباره امتحان کنید

---

## 📞 کمک بیشتر

اگر همچنان مشکل دارید:

1. خروجی کامل خطا را کپی کنید
2. فایل `app.log` را بررسی کنید
3. مطمئن شوید که:
   - ✅ Python 3.8 یا بالاتر نصب است
   - ✅ فایروال Python را block نمی‌کند
   - ✅ آنتی‌ویروس Python را block نمی‌کند

---

## 🎉 تبریک!

اگر همه چیز درست پیش رفت، باید صفحه زیبای داشبورد بات را ببینید! 🚀

