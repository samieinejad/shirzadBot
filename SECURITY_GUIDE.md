# 🔒 Security Guide - حفاظت از توکن‌ها

## ⚠️ **هشدار مهم: توکن‌های شما در معرض خطر هستند!**

---

## 🚨 **مشکل فعلی**

در حال حاضر توکن‌های واقعی شما در فایل `app.py` هستند و **قابل مشاهده** برای همه!

---

## ✅ **راه حل: استفاده از config.py**

### **مرحله ۱: ایجاد فایل config.py**

```bash
# کپی کردن فایل example
copy config.example.py config.py

# یا در Linux:
cp config.example.py config.py
```

### **مرحله ۲: تنظیم توکن‌های واقعی**

فایل `config.py` را باز کنید و توکن‌های واقعی را وارد کنید:

```python
TELEGRAM_BOT_TOKEN = "YOUR_TELEGRAM_TOKEN_HERE"
BALE_BOT_TOKEN = "YOUR_BALE_TOKEN_HERE"
ITA_BOT_TOKEN = "YOUR_ITA_TOKEN_HERE"

OWNER_ID = YOUR_OWNER_ID
BALE_OWNER_ID = YOUR_BALE_OWNER_ID
ITA_OWNER_ID = "YOUR_ITA_OWNER_ID"
```

### **مرحله ۳: تأیید**

```bash
# اجرا کنید و ببینید
python app.py

# باید این پیام را ببینید:
# ✅ Configuration loaded from config.py
```

---

## 🛡️ **حفاظت از Git**

### **فایل‌های محافظت شده:**
- ✅ `config.py` (دارای توکن‌ها) - در `.gitignore` است
- ✅ `.env` (اگر استفاده شود)
- ✅ `*.db` (دیتابیس‌ها)
- ✅ `*.log` (لاگ‌ها)

### **فایل‌های امن برای Push:**
- ✅ `app.py` (بدون توکن‌های واقعی)
- ✅ `config.example.py` (بدون توکن‌های واقعی)
- ✅ `index.html`
- ✅ همه Documentation

---

## 📋 **چک‌لیست امنیتی**

### **قبل از Push به Git:**

- [ ] `config.py` ایجاد شد
- [ ] توکن‌های واقعی در `config.py` هستند
- [ ] `config.py` در `.gitignore` است
- [ ] توکن‌های واقعی از `app.py` حذف شدند
- [ ] `git status` را چک کردید و `config.py` نشان نمی‌دهد
- [ ] Test کردید که برنامه با `config.py` کار می‌کند

### **بررسی:**

```bash
# بررسی که config.py در Git نیست
git status

# نباید config.py در لیست باشد!
# اگر بود:
git reset HEAD config.py
```

---

## 🔄 **روش صحیح Deployment**

### **روی Local Machine (Windows):**
```bash
# 1. ایجاد config.py
copy config.example.py config.py

# 2. تنظیم توکن‌ها
# باز کردن config.py و ویرایش

# 3. اجرا
python app.py
```

### **روی Production Server (Ubuntu):**
```bash
# 1. Clone از Git
git clone <YOUR_REPO_URL>

# 2. ایجاد config.py
cp config.example.py config.py

# 3. تنظیم توکن‌ها
nano config.py

# 4. اجرا
python app.py
```

---

## ⚡ **Quick Fix (الان!):**

اگر توکن‌های شما در `app.py` هستند و می‌خواهید فوراً ایمن کنید:

### **گام ۱:**
```bash
copy config.example.py config.py
```

### **گام ۲:**
فایل `config.py` را ویرایش کنید و توکن‌های واقعی را بگذارید

### **گام ۳:**
تست کنید:
```bash
python app.py
```

اگر پیام "✅ Configuration loaded from config.py" را دیدید، موفق بودید!

### **گام ۴:**
در `.gitignore` بررسی کنید که `config.py` باشد

### **گام ۵:**
```bash
git status
```

اگر `config.py` در لیست نبود، همه چیز امن است!

---

## 🔐 **بهترین روش‌های امنیتی**

### ۱. **Environment Variables (بهترین روش):**
```python
import os
TELEGRAM_BOT_TOKEN = os.getenv('TELEGRAM_BOT_TOKEN')
```

### ۲. **config.py (روش فعلی ما):**
```python
import config
TELEGRAM_BOT_TOKEN = config.TELEGRAM_BOT_TOKEN
```

### ۳. **هرگز در کد مستقیماً:**
```python
# ❌ بد
TELEGRAM_BOT_TOKEN = "YOUR_ACTUAL_TOKEN_IN_CODE"
```

---

## 🆘 **اگر توکن‌ها لو رفت**

### **اگر توکن‌های شما لو رفته:**

1. ✅ **فوراً Revoke کنید:**
   - Telegram: @BotFather → /revoke
   - Bale: پنل توسعه‌دهنده
   - Ita: پنل ادمین

2. ✅ **توکن‌های جدید بگیرید**

3. ✅ **بررسی کنید کجا لو رفته**

4. ✅ **توکن‌های جدید را در config.py قرار دهید**

---

## 📝 **یادداشت مهم**

- ✅ `config.py` هرگز Push نکنید
- ✅ `config.example.py` را Push کنید (بدون توکن‌های واقعی)
- ✅ `.gitignore` را چک کنید
- ✅ Backup از `config.py` بگیرید
- ✅ تغییر دسترسی به `config.py` را محدود کنید

---

**با این روش، توکن‌های شما امن می‌مانند! 🔐**

