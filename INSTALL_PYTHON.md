# 🐍 نصب Python - راهنمای تصویری

## ⚠️ Python نصب نشده است!

Python روی کامپیوتر شما نصب نیست. قبل از اجرای برنامه، باید Python را نصب کنید.

---

## ✅ روش ساده: نصب از Microsoft Store

### گام ۱: باز کردن Store
1. دکمه `Windows` را فشار دهید
2. "Microsoft Store" را تایپ کنید و Enter بزنید

### گام ۲: جستجو و نصب
1. در Store، "Python 3.11" را جستجو کنید
2. "Python 3.11" از Python Software Foundation را انتخاب کنید
3. روی "Install" کلیک کنید
4. منتظر بمانید تا نصب تمام شود

### گام ۳: تأیید نصب
PowerShell جدید باز کنید:
```
Win + X > Windows PowerShell
```
سپس:
```
python --version
```

---

## ✅ روش حرفه‌ای: نصب از Python.org

### گام ۱: دانلود
1. به آدرس زیر بروید:
   ```
   https://www.python.org/downloads/
   ```

2. روی دکمه بزرگ "Download Python 3.11.x" کلیک کنید

### گام ۲: نصب مهم!
1. فایل دانلود شده را اجرا کنید
2. ⚠️ **حتما** تیک زیر را فعال کنید:
   ```
   ☑️ Add Python 3.11 to PATH
   ```
3. روی "Install Now" کلیک کنید
4. منتظر بمانید

### گام ۳: بررسی
PowerShell جدید باز کنید و چک کنید:
```
python --version
pip --version
```

---

## 🎉 پس از نصب Python

حالا می‌توانید برنامه را اجرا کنید:

### نصب وابستگی‌ها:
```batch
install_simple.bat
```

### اجرای برنامه:
```batch
run_simple.bat
```

### باز کردن در مرورگر:
```
http://localhost:5000
```

---

## ❓ سوالات متداول

**سوال:** کدام نسخه Python؟
**جواب:** هر نسخه 3.8 یا بالاتر مناسب است. نسخه 3.11 یا 3.12 توصیه می‌شود.

**سوال:** چرا "Add to PATH" مهم است؟
**جواب:** بدون آن، نمی‌توانید `python` را در PowerShell اجرا کنید.

**سوال:** بعد از نصب Python چه کنم؟
**جواب:** PowerShell را ببندید و دوباره باز کنید. سپس `python --version` را اجرا کنید.

**سوال:** خطای "python is not recognized"؟
**جواب:** 
1. PowerShell را ببندید و دوباره باز کنید
2. اگر نشد، Python را Uninstall و دوباره نصب کنید
3. حتماً "Add to PATH" را فعال کنید

---

## 🆘 مشکل دارید؟

اگر پس از نصب Python هنوز مشکلی دارید:

1. **بررسی PATH:**
   - Win + R > `sysdm.cpl` > Advanced > Environment Variables
   - در "System variables" دنبال "Path" بگردید
   - مطمئن شوید مسیر Python وجود دارد

2. **نصب مجدد:**
   - Python را Uninstall کنید
   - کامپیوتر را Restart کنید
   - دوباره نصب کنید با "Add to PATH"

3. **استفاده از CMD:**
   - به جای PowerShell از Command Prompt استفاده کنید
   - ممکن است PATH متفاوت باشد

---

**موفق باشید! 🎉**

