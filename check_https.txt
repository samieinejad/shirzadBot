Run these commands:

# 1. Check if SSL is configured
curl https://social.msn1.ir -k | head -20

# 2. Check if SSL cert is valid
sudo certbot certificates

# 3. List all Nginx sites
ls -la /etc/nginx/sites-enabled/

# 4. Check if HTTPS is redirecting properly
curl -I http://social.msn1.ir
curl -I https://social.msn1.ir

